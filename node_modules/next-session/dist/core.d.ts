import Session from './session';
import { Options, Request, Response } from './types';
export declare function stringify(sess: Session): string;
export declare function applySession(req: Request, res: Response, opts?: Options): Promise<void>;
